import { mapCreaterActions } from "./mapCreater.actions";
import { mapCreaterReducer } from "./mapCreater.reducer";

export const mapCreater = {
    actions: mapCreaterActions,
    reducer: mapCreaterReducer
}